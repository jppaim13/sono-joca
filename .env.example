# Só para rodar no seu computador. No Render estas variáveis são criadas pelo render.yaml.
FLASK_APP=app
SECRET_KEY=troque-por-um-valor-aleatorio
# Sem DATABASE_URL, usa SQLite local (dev.db)
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/sono
